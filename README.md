Markdown
# 🛡️ Remna SysLab Backup & Restore Tool

**Автономная система резервного копирования и восстановления для Docker-проектов.**

![Bash](https://img.shields.io/badge/Language-Bash-4EAA25?style=flat-square&logo=gnu-bash)
![Docker](https://img.shields.io/badge/Platform-Docker-2496ED?style=flat-square&logo=docker)
![Telegram](https://img.shields.io/badge/Notification-Telegram-2CA5E0?style=flat-square&logo=telegram)
![License](https://img.shields.io/badge/License-MIT-blue?style=flat-square)

Этот инструмент разработан для автоматического создания бэкапов базы данных (PostgreSQL) и конфигурационных файлов (`.env`), их безопасной отправки в закрытый Telegram-канал и быстрого восстановления системы в случае сбоя.

---

## 🚀 Возможности

* **📦 Полный бэкап:** Создает дамп базы данных PostgreSQL и архивирует его вместе с файлом `.env`.
* **☁️ Облачное хранение:** Мгновенная отправка архивов в Telegram (поддержка топиков/thread_id).
* **🚨 Мониторинг ошибок:** Если бэкап не удался (ошибка базы, нет места, сбой сети), скрипт отправит тревожное уведомление.
* **♻️ Авто-ротация:** Удаляет локальные бэкапы старше указанного срока (по умолчанию 14 дней).
* **🔄 Самообновление:** Обновление скрипта с GitHub одной кнопкой без потери настроек.
* **⚡ Быстрое восстановление:** Интерактивное меню для развертывания бэкапа одной командой.
* **⏰ Планировщик:** Удобная настройка Cron-расписания через меню.

---

## 📥 Установка (в одну строку)

Зайдите на сервер по SSH и выполните команду (требуются права `sudo`):

```bash
curl -sSL [https://raw.githubusercontent.com/SeshiStrikles/remna-syslab-backup-restore/main/remna-syslab-backup-restore.sh](https://raw.githubusercontent.com/SeshiStrikles/remna-syslab-backup-restore/main/remna-syslab-backup-restore.sh) -o install_backup.sh && chmod +x install_backup.sh && sudo ./install_backup.sh
Что произойдет:

Скрипт скачается и запустит мастер установки.

Попросит указать путь к вашему проекту (по умолчанию /opt/VpnManager).

Запросит Telegram Bot Token и Chat ID.

Создаст рабочую директорию /opt/remna-syslab-backup-restore и установится туда.

⚙️ Использование
После установки скрипт можно запускать из любой директории (если добавить alias) или напрямую:

Bash
sudo /opt/remna-syslab-backup-restore/remna-syslab-backup-restore.sh
Откроется интерактивное меню:

Plaintext
=== Remna SysLab Backup Manager v3.1 ===
1. 🚀 Бэкап сейчас          # Создать и отправить бэкап вручную
2. ♻️  Восстановить          # Меню восстановления из локального архива
3. ⏰ Настроить Cron        # Установить расписание авто-бэкапов
4. ⚙️  Показать настройки    # Показать текущие пути и токены
5. 🛠  Изменить настройки    # Изменить токен, чат или срок хранения
6. 🔄 Обновить скрипт       # Скачать новую версию с GitHub
7. ❌ Удалить менеджер      # Полное удаление скрипта
0. Выход
Автоматический режим (Cron)
Скрипт поддерживает запуск с флагом --auto для работы в Cron (без меню):

Bash
/opt/remna-syslab-backup-restore/remna-syslab-backup-restore.sh --auto
🆘 Аварийное восстановление
Если сервер упал или база данных повреждена:

Запустите скрипт.

Выберите пункт 2. ♻️ Восстановить.

Выберите нужный файл бэкапа из списка.

Подтвердите действие.

Скрипт автоматически:

Остановит Docker-контейнер бота.

Распакует архив.

Восстановит базу данных из SQL-дампа.

Предложит восстановить файл .env.

Запустит бота обратно.

📋 Требования
OS: Linux (Ubuntu/Debian/CentOS)

Установленные пакеты: curl, zip, cron.

Docker & Docker Compose.

Проект должен иметь структуру с файлом .env и контейнером БД.

🔒 Безопасность
Токены и пароли хранятся внутри самого скрипта в директории /opt/remna-syslab-backup-restore, доступной только root.

При отправке в Telegram файлы передаются по HTTPS.
